/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BasicHighlight} from './basic-highlight/basic-highlight.directive.ts';

describe('Directive: BasicHighlight.directive.spec.ts', () => {
  it('should create an instance', () => {
    const directive = new BasicHighlight.directive.spec.tsDirective();
    expect(directive).toBeTruthy();
  });
});
